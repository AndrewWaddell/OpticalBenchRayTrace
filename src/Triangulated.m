classdef Triangulated < Shape
    properties
        p
        cm
    end
    methods
        function change_of_basis(obj)
        end
        function triangle_interior(obj)
        end
        function point_of_intersection(obj)
        end
        function triangle_points(obj)
            %use cm and p to give points from triangle index
        end
    end
end